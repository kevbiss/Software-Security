#include <stdio.h>

int main(){
  int modified = 0x12341234;
  char name[107] ={0};
  read (0,name, 0x107);
  if (modified != 0X12341234)
  puts ("the variable was changed successfully")
  puts("You have passed the ctf congratulations")
  }else{
  put("Error the variable wasn't change")
}
}
